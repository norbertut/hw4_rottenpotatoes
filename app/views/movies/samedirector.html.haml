-#  This file is app/views/movies/samedirector.html.haml
%h1 Movies with Same Director #{@director}
-#recibe 1 como parámetro y tiene que escoger las pelis con el mismo director que este movie id


%table#movies
  %thead
    %tr
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Back to movie list', movies_path
